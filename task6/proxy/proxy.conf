server {
    listen 80;

    resolver 127.0.0.11 valid=10s ipv6=off;

    location /api {
        set $backend http://back-end:5252;
        proxy_pass $backend;
    }

    location / {
        proxy_pass http://front-end:9000;
    }
}